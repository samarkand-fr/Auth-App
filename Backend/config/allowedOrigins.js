
// Array containing allowed origins for CORS (Cross-Origin Resource Sharing)
const allowedOrigins = ["http://localhost:3000"];

// Exporting the allowedOrigins array for use in other modules
module.exports = allowedOrigins;
